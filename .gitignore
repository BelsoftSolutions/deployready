# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*

# DeployReady local state & generated reports
.deployready/
deployready-report.md
deployready-report.html

# Backups created by the interactive fix flow
*.bak.*

# Environment & secrets — never commit
.env
.env.*
*.pem
*.key
.npmrc
# ...except this fake .env test fixture (no real secrets) the test suite scans
!tests/sample-app/.env

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Internal-only (never publish) — source-of-truth, session handoff & Claude workspace
STATUS.md
CLAUDE.md
.claude/

# Baseline file produced by the CI gate (project-specific, not ours to ship)
.deployready-baseline.json

# Test coverage
coverage/
