/**
 * Phase 1.5 — Handles propose_fix → show diff → get approval → apply_fix →
 * rerun_test. Enforces: apply only after explicit approval, one file per
 * approval, always re-verify. Stub for the MVP.
 */
export class FixManager {
  async proposeAndApply(): Promise<never> {
    throw new Error('Agentic fix flow ships in Phase 1.5.');
  }
}
